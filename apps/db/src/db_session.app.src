{application, db_session, [
    {description, "db session application."},
    {vsn, "0.0.1"},
    {modules, [db_session_app, db_session_sup, db_session]},
    {registered, [db_session_sup]},
    {mod, {db_session_app, [] } },
    {applications, [emysql]},
    {env, []}
]
}.
